title: Quick Start
description: Get up and running with Supercheck in 5 minutes
---

# Quick Start

Get your first test running in under 5 minutes.

## Create Your Account

<Steps>
  <Step>Sign up at [supercheck.io](https://supercheck.io)</Step>
  <Step>Verify your email address</Step>
  <Step>Create your first workspace</Step>
</Steps>

## Run Your First Test

<Tabs items={['Browser Test', 'API Test', 'Database Test']}>
  <Tab value="Browser Test">
    ```typescript
    // Navigate to your application
    await page.goto('https://your-app.com');

    // Interact with the page
    await page.click('button[type="submit"]');

    // Verify the result
    await expect(page.locator('.success')).toBeVisible();
    ```
  </Tab>
  <Tab value="API Test">
    ```typescript
    // Make an API request
    const response = await fetch('https://api.example.com/users');

    // Check the response
    expect(response.status).toBe(200);
    const data = await response.json();
    expect(data.users).toHaveLength(10);
    ```
  </Tab>
  <Tab value="Database Test">
    ```sql
    -- Query your database
    SELECT COUNT(*) as user_count FROM users WHERE active = true;

    -- Expected: user_count > 0
    ```
  </Tab>
</Tabs>

## What's Next?

<Cards>
  <Card
    title="Core Concepts"
    description="Understand tests, monitors, jobs, and runs"
    href="./concepts"
  />
  <Card
    title="Testing Guide"
    description="Deep dive into browser, API, and database testing"
    href="./testing"
  />
  <Card
    title="Set Up Monitoring"
    description="Configure 24/7 monitoring and alerts"
    href="./monitoring"
  />
</Cards>
