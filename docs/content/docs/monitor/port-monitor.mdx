---
title: Port Monitor
description: Monitor TCP/UDP port availability
icon: Network
---

# Port Monitor

Monitor TCP/UDP ports to ensure your services are accessible on the network.

## Port Monitoring Flow

```mermaid
flowchart TB
    A[Port Monitor] --> B{Protocol Type}
    B -->|TCP| C[TCP Connection Attempt]
    B -->|UDP| D[UDP Packet Send]

    C --> E{Connection Status}
    E -->|Success| F[Measure Connection Time]
    E -->|Refused| G[✗ Port Closed]
    E -->|Timeout| H[✗ No Response]

    D --> I{UDP Response}
    I -->|Received| J[✓ Port Open]
    I -->|ICMP Error| K[✗ Port Unreachable]
    I -->|Timeout| L[⚠ No Response]

    F --> M{Response Time}
    M -->|< 100ms| N[✓ Fast]
    M -->|100-500ms| O[⚠ Moderate]
    M -->|> 500ms| P[✗ Slow]

    G --> Q[Update Status]
    H --> Q
    J --> Q
    K --> Q
    L --> Q
    N --> Q
    O --> Q
    P --> Q

    Q --> R{Consecutive Failures?}
    R -->|>= 3| S[Send Alert]
    R -->|< 3| T[Continue Monitoring]

    S --> U[Notify Team]
    U --> V{Port Recovers?}
    V -->|Yes| W[Recovery Alert]
    V -->|No| T

    style A fill:#3b82f6,color:#fff
    style N fill:#10b981,color:#fff
    style J fill:#10b981,color:#fff
    style O fill:#f59e0b,color:#fff
    style L fill:#f59e0b,color:#fff
    style G fill:#ef4444,color:#fff
    style H fill:#ef4444,color:#fff
    style K fill:#ef4444,color:#fff
    style P fill:#ef4444,color:#fff
    style S fill:#ef4444,color:#fff
    style W fill:#10b981,color:#fff
```

## Features

- TCP/UDP port checking
- Connection time monitoring
- Service availability tracking
- Protocol validation
- Multi-location testing

## Creating a Port Monitor

```mermaid
graph LR
    A[Start] --> B[Navigate to Monitor]
    B --> C[Click Create]
    C --> D[Select Port Monitor]
    D --> E[Enter Hostname/IP]
    E --> F[Enter Port Number]
    F --> G[Select Protocol]
    G --> H[Configure Interval]
    H --> I[Set Timeout]
    I --> J[Choose Locations]
    J --> K[Save Monitor]
    K --> L[✓ Monitoring Active]

    style A fill:#3b82f6,color:#fff
    style L fill:#10b981,color:#fff
    style D fill:#8b5cf6,color:#fff
    style G fill:#f59e0b,color:#fff
```

<Steps>
  <Step>Navigate to **Monitor → Create → Port Monitor**</Step>
  <Step>Enter hostname/IP and port number</Step>
  <Step>Select protocol (TCP/UDP)</Step>
  <Step>Configure check interval (1-60 minutes)</Step>
  <Step>Set timeout and retry settings</Step>
</Steps>

## Protocol Comparison

```mermaid
graph TB
    A[Port Monitoring] --> B{Protocol Selection}

    B --> C[TCP Monitoring]
    B --> D[UDP Monitoring]

    C --> C1[Connection-Oriented]
    C --> C2[Guaranteed Delivery]
    C --> C3[Clear Open/Closed State]
    C --> C4[Use Cases]

    C4 --> C5[Web Servers: 80, 443]
    C4 --> C6[SSH: 22]
    C4 --> C7[Database: 3306, 5432]
    C4 --> C8[SMTP: 25, 587]

    D --> D1[Connectionless]
    D --> D2[No Delivery Guarantee]
    D --> D3[Stateless]
    D --> D4[Use Cases]

    D4 --> D5[DNS: 53]
    D4 --> D6[NTP: 123]
    D4 --> D7[SNMP: 161]
    D4 --> D8[VoIP/Gaming]

    style A fill:#3b82f6,color:#fff
    style C fill:#10b981,color:#fff
    style D fill:#8b5cf6,color:#fff
    style C4 fill:#f59e0b,color:#fff
    style D4 fill:#f59e0b,color:#fff
```

## Best Practices

- Monitor critical service ports (80, 443, 22, 3306, etc.)
- Set appropriate timeouts based on protocol
  - TCP: 5-10 seconds
  - UDP: 10-15 seconds (stateless nature)
- Monitor from multiple locations to detect regional issues
- Track connection time trends for performance insights
- Use TCP for most services (more reliable)
- Use UDP for DNS, NTP, and other stateless protocols
- Configure alerts for consecutive failures (3+)
