---
title: Custom Test
description: Write custom Node.js test scripts
icon: SquareFunction
---

Write custom test scripts for advanced scenarios.

## Create Test

1. Go to **Create → Custom Test**
2. Write TypeScript/JavaScript code
3. Run and debug
4. Save when passing

## Example

```typescript
import { test, expect } from '@supercheck/test';

test('workflow test', async () => {
  // Create user
  const res = await fetch('https://api.example.com/users', {
    method: 'POST',
    body: JSON.stringify({ name: 'Test User' })
  });
  
  const user = await res.json();
  expect(user.id).toBeDefined();
  
  // Verify user
  const getRes = await fetch(`https://api.example.com/users/${user.id}`);
  const fetched = await getRes.json();
  expect(fetched.name).toBe('Test User');
});
```

## Features

- Full Node.js environment
- Popular npm packages available
- Access to variables and secrets
- Custom assertions
