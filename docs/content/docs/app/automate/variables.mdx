---
title: Variables
description: Store configuration, secrets, and data files securely
icon: Variable
---

Variables let you store configuration values, sensitive credentials, and data files that your tests can access at runtime. Use variables for URLs, timeouts, and settings. Use secrets for API keys, passwords, and tokens that need encryption. Use file variables for CSV data, JSON fixtures, and configuration files.

![Variables List](/screenshots/variables-list.png)

## Variables vs Secrets vs Files

<Tabs items={['Variables', 'Secrets', 'Files']}>
<Tab value="Variables">

- **Storage:** Plain text
- **Visibility:** Shown in UI
- **Logging:** Logged normally
- **Use For:** URLs, timeouts, feature flags

</Tab>
<Tab value="Secrets">

- **Storage:** AES-128-GCM encrypted
- **Visibility:** Masked with `***`
- **Logging:** Avoid intentional logging; execution output is redacted
- **Use For:** API keys, passwords, tokens

</Tab>
<Tab value="Files">

- **Storage:** S3/MinIO object storage
- **Max Size:** 5 MB
- **Supported Types:** CSV, JSON, YAML, XML, TSV, plain text
- **Access:** `getFile(key)` returns the file path inside the container
- **Use For:** Test data sets, configuration files, fixtures

</Tab>
</Tabs>

## Creating Variables

<Tabs items={['Add Variable', 'Add Secret', 'Add File']}>
<Tab value="Add Variable">

1. Go to your project's **Variables** page
2. Click **Add Variable**
3. Select the **Variable** type (default)
4. Enter a **key** (e.g., `BASE_URL`)
5. Enter the **value** (e.g., `https://staging.example.com`)
6. Optionally add a **description** for your team
7. Click **Add Variable**

![Add Variable](/screenshots/add-variable.png)

</Tab>
<Tab value="Add Secret">

1. Go to your project's **Variables** page
2. Click **Add Variable**
3. Select the **Secret** type from the Type dropdown
4. Enter a **key** (e.g., `API_KEY`)
5. Enter the **value**
6. Optionally add a **description**
7. Click **Add Variable**

![Add Secret](/screenshots/add-secret.png)

<Callout type="info">
Secret values are masked by default in the UI. Users with secret-view permission can explicitly reveal a value via the dedicated action.
</Callout>

</Tab>
<Tab value="Add File">

1. Go to your project's **Variables** page
2. Click **Add Variable**
3. Select the **File** type
4. Enter a **key** (e.g., `TEST_DATA`)
5. Upload a file (CSV, JSON, YAML, XML, TSV, or plain text — max 5 MB)
6. Optionally add a **description**
7. Click **Add Variable**

<Callout type="info">
File variables are stored in S3/MinIO and made available inside the test execution container at runtime. Use `getFile(key)` to get the file path, then read it with standard file I/O.
</Callout>

</Tab>
</Tabs>

## Using Variables in Tests

Access variables and secrets in your test code using the built-in functions:

<Tabs items={['getVariable()', 'getSecret()', 'getFile()']}>
<Tab value="getVariable()">

Use for non-sensitive configuration:

```javascript
/**
 * Variables store plain-text configuration values.
 * Use getVariable() to retrieve them in your tests.
 */

// Basic usage
const baseUrl = getVariable('BASE_URL');
const timeout = getVariable('TIMEOUT');

// Navigate using variable
await page.goto(baseUrl + '/login');
```

</Tab>
<Tab value="getSecret()">

Use for sensitive credentials:

```javascript
/**
 * Secrets are encrypted and resolved at runtime.
 * Secret values are automatically redacted from logs.
 */

// API authentication
const apiKey = getSecret('API_KEY');
await page.setExtraHTTPHeaders({
  'Authorization': `Bearer ${apiKey}`
});

// Form login
const password = getSecret('TEST_PASSWORD');
await page.fill('#password', password);

// Database connection
const dbPassword = getSecret('DB_PASSWORD');
```

</Tab>
<Tab value="getFile()">

Use for data files uploaded as file variables:

```javascript
import fs from 'fs';

/**
 * File variables are uploaded via the Variables page.
 * getFile() returns the absolute path to the file inside the container.
 * Read the file using standard Node.js fs module.
 */

// Get path to the uploaded CSV file
const csvPath = getFile('TEST_DATA');

// Read and parse CSV
const content = fs.readFileSync(csvPath, 'utf-8');
const rows = content.split('\n').map(row => row.split(','));
const headers = rows[0];
const data = rows.slice(1).filter(row => row.length === headers.length);

// Use data in tests
for (const row of data) {
  await page.fill('#name', row[0]);
  await page.fill('#email', row[1]);
  await page.click('button[type="submit"]');
}
```

</Tab>
</Tabs>

### Secret Protection

Secrets are resolved at runtime and execution output is redacted before persistence/return:

```javascript
/**
 * Secret values are injected at runtime.
 * The actual values are resolved at runtime.
 */

// Avoid intentional secret logging in scripts
// Worker redaction is an additional protection layer for accidental output
console.log('API key configured:', !!getSecret('API_KEY'));

const actualKey = getSecret('API_KEY');
```

**Secrets work normally in:**
- Playwright actions (`page.fill()`, `page.click()`)
- HTTP headers and request bodies
- URL parameters
- Comparisons and conditionals

## Common Patterns

<Tabs items={['Environment-Based', 'API Testing', 'Database Testing', 'Feature Flags', 'k6 Performance', 'Data-Driven (File)']}>
<Tab value="Environment-Based">

```javascript
/**
 * Load environment-specific configuration.
 * Store different URLs for staging vs production.
 */
const env = getVariable('ENVIRONMENT'); // 'staging' or 'production'
const baseUrl = getVariable(`${env.toUpperCase()}_URL`);

await page.goto(baseUrl);
```

</Tab>
<Tab value="API Testing">

```javascript
/**
 * Authenticated API request using Playwright's request context.
 * @see https://playwright.dev/docs/api-testing
 */
const apiUrl = getVariable('API_URL');
const apiToken = getSecret('API_TOKEN');
const apiKey = getSecret('API_KEY');

const response = await request.get(apiUrl + '/users', {
  headers: {
    'Authorization': `Bearer ${apiToken}`,
    'X-API-Key': apiKey
  }
});
```

</Tab>
<Tab value="Database Testing">

```javascript
/**
 * PostgreSQL database connection with secure credentials.
 * @requires pg - PostgreSQL client for Node.js
 */
import { Pool } from 'pg';

const pool = new Pool({
  host: getVariable('DB_HOST'),
  port: parseInt(getVariable('DB_PORT')),
  database: getVariable('DB_NAME'),
  user: getVariable('DB_USER'),
  password: getSecret('DB_PASSWORD')
});
```

</Tab>
<Tab value="Feature Flags">

```javascript
/**
 * Conditional test logic based on feature flags.
 * Useful for testing different UI versions.
 */
const featureEnabled = getVariable('ENABLE_NEW_CHECKOUT') === 'true';

if (featureEnabled) {
  await page.click('[data-testid="new-checkout"]');
} else {
  await page.click('[data-testid="classic-checkout"]');
}
```

</Tab>
<Tab value="k6 Performance">

```javascript
/**
 * Using variables in k6 load tests.
 * Variables are injected at runtime.
 * @see https://grafana.com/docs/k6/latest/using-k6/http-requests/
 */
import http from 'k6/http';

export default function () {
  const baseUrl = getVariable('API_URL');
  const apiKey = getSecret('API_KEY');
  
  http.get(`${baseUrl}/protected`, {
    headers: { Authorization: `Bearer ${apiKey}` }
  });
}
```

</Tab>
<Tab value="Data-Driven (File)">

```javascript
import fs from 'fs';

/**
 * Data-driven testing using a CSV file variable.
 * Upload your test data CSV via Variables page as a file variable.
 */
const csvPath = getFile('USER_DATA');
const csv = fs.readFileSync(csvPath, 'utf-8');
const users = csv
  .split('\n')
  .slice(1) // Skip header row
  .filter(line => line.trim())
  .map(line => {
    const [name, email, role] = line.split(',');
    return { name, email, role };
  });

// Test each user from the data file
for (const user of users) {
  await page.goto(getVariable('BASE_URL') + '/admin/users');
  await page.click('button:has-text("Add User")');
  await page.fill('#name', user.name);
  await page.fill('#email', user.email);
  await page.selectOption('#role', user.role);
  await page.click('button[type="submit"]');
  await expect(page.locator('.toast-success')).toBeVisible();
}
```

</Tab>
</Tabs>

## Variable Naming Conventions

Use clear, consistent naming for your variables:

| Pattern | Example | Use For |
|---------|---------|---------|
| `SCREAMING_SNAKE_CASE` | `BASE_URL`, `API_KEY` | Standard convention |
| `ENV_SPECIFIC` | `STAGING_URL`, `PROD_API_KEY` | Environment-specific values |
| `SERVICE_NAME` | `STRIPE_API_KEY`, `SENDGRID_KEY` | Third-party service credentials |

**Recommended variable names:**

| Variable | Description |
|----------|-------------|
| `BASE_URL` | Application base URL |
| `API_URL` | API endpoint base URL |
| `TIMEOUT` | Default timeout in milliseconds |
| `TEST_USER_EMAIL` | Test account email |
| `TEST_USER_PASSWORD` | Test account password (secret) |
| `API_KEY` | API authentication key (secret) |
| `DB_CONNECTION_STRING` | Database connection (secret) |

## Security Features

<Tabs items={['Encryption', 'Access Control', 'Audit Trail']}>
<Tab value="Encryption">

- Secrets are encrypted using **AES-128-GCM** authenticated encryption (Node.js `crypto`)
- Encryption keys are derived per-project using HKDF-SHA256
- Values are encrypted at rest in the database

</Tab>
<Tab value="Access Control">

Variables follow your project's role-based permissions:

| Role | View | Create | Edit | Delete | View Secret Values |
|------|------|--------|------|--------|-------------------|
| **Owner** | ✅ | ✅ | ✅ | ✅ | ✅ |
| **Admin** | ✅ | ✅ | ✅ | ✅ | ✅ |
| **Editor** | ✅ | ✅ | ✅ | ❌ | ✅ |
| **Viewer** | ✅ | ❌ | ❌ | ❌ | ❌ |

</Tab>
<Tab value="Audit Trail">

All variable operations are logged:
- Who created, updated, or deleted variables
- When changes were made
- Variable keys (not secret values)

</Tab>
</Tabs>
