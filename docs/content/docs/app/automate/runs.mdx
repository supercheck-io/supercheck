---
title: Runs
description: View execution history and results
icon: ClipboardList
---

Every test or job execution creates a run with results, logs, and artifacts.

![Job Runs](/screenshots/job-runs.png)

## Run Status

| Status | Description |
|--------|-------------|
| **Passed** | All tests completed successfully |
| **Failed** | One or more tests failed |
| **Error** | Execution error (timeout, crash, infrastructure) |
| **Running** | Currently executing |
| **Queued** | Waiting to execute |
| **Blocked** | Blocked by plan limits |

```mermaid
%%{init: {"theme":"base","themeVariables":{"fontFamily":"ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, sans-serif","primaryColor":"#6366f1","primaryTextColor":"#ffffff","primaryBorderColor":"#4338ca","lineColor":"#94a3b8","actorBkg":"transparent","actorBorder":"transparent","actorTextColor":"#ffffff","actorLineColor":"#94a3b8","signalColor":"#6366f1","signalTextColor":"#64748b","labelBoxBkgColor":"#d97706","labelBoxBorderColor":"#b45309","labelTextColor":"#ffffff","loopTextColor":"#64748b","activationBkgColor":"#7c3aed","activationBorderColor":"#6d28d9","noteBkgColor":"#d97706","noteBorderColor":"#b45309","noteTextColor":"#ffffff","textColor":"#64748b","mainBkg":"#ffffff"}}}%%
sequenceDiagram
    box rgb(79,70,229)
        participant You
    end
    box rgb(37,99,235)
        participant App as Supercheck
    end
    box rgb(220,38,38)
        participant Queue as Queue
    end
    box rgb(5,150,105)
        participant Worker
    end
    box rgb(13,148,136)
        participant Sandbox as K3s + gVisor
    end

    You->>App: Trigger a test or job
    App->>Queue: Enqueue run
    Queue->>Worker: Dequeue run
    activate Worker
    Worker->>Sandbox: Execute in isolated container
    Sandbox-->>Worker: Results and artifacts
    Worker->>App: Mark Passed / Failed / Error
    deactivate Worker
    App-->>You: Live updates, then results and artifacts
    opt Cancel a running run
        You->>App: Cancel
        App->>Worker: Signal cancellation
        App->>App: Record run as Error (cancelled)
    end
```

## View Runs

1. Go to **Runs**
2. Select a run to view its details

When a running execution is cancelled, Supercheck stops it and records the run as **Error** with cancellation details.

## Run Details

Each run includes:

- **Results** — Pass/fail count and duration
- **Screenshots** — Visual state at each step
- **Videos** — Browser test recordings
- **Logs** — Console output
- **Trace** — Step-by-step execution replay

## Debug Failures

1. Open failed run
2. Check error message
3. View screenshot at failure point
4. Review console logs
5. Check network requests

## AI Analyze

Click **AI Analyze** on any job run to generate an AI-powered analysis report.

**What AI Analyze Provides:**
- **Execution Summary** — Overview of what happened during the run
- **Failure Analysis** — Root cause identification with specific error messages
- **Performance Insights** — Duration analysis and optimization suggestions
- **Recommendations** — Actionable next steps based on actual run data

**Supported Job Types:**
- **Playwright** — Analyzes test failures with HTML report context
- **K6** — Analyzes performance metrics and threshold breaches

<Callout type="info">
AI Analyze requires a configured AI provider. See [Deployment](/docs/app/deployment/self-hosted#optional-configuration) for setup.
</Callout>
