# SuperCheck E2E Test Environment Variables
# Copy this file to .env and fill in the values

# =============================================================================
# BASE CONFIGURATION
# =============================================================================

# Base URL for the staging/production environment
E2E_BASE_URL=https://demo.supercheck.io

# =============================================================================
# TEST USER CREDENTIALS
# =============================================================================

# Primary test user credentials (must be an invited user with email/password)
# Create this user via Settings → Members → Invite
E2E_TEST_USER_EMAIL=e2e-test@example.com
E2E_TEST_USER_PASSWORD=your-secure-password

# =============================================================================
# RBAC TEST USERS (Optional - for role-based access control tests)
# =============================================================================
# These users are required to run the full RBAC test suite.
# Each user should be invited to the same organization with the specified role.
# Create these users via Settings → Members → Invite with appropriate roles.

# Super Admin user (platform-wide access)
# Note: Super Admin is granted via CLI: npm run setup:admin <email>
E2E_SUPER_ADMIN_EMAIL=
E2E_SUPER_ADMIN_PASSWORD=

# Organization Owner user (full org access including billing)
E2E_ORG_OWNER_EMAIL=
E2E_ORG_OWNER_PASSWORD=

# Organization Admin user (org administration, no billing)
E2E_ORG_ADMIN_EMAIL=
E2E_ORG_ADMIN_PASSWORD=

# Project Admin user (project-level administration)
E2E_PROJECT_ADMIN_EMAIL=
E2E_PROJECT_ADMIN_PASSWORD=

# Editor user (can create and modify resources)
E2E_EDITOR_EMAIL=
E2E_EDITOR_PASSWORD=

# Viewer user (read-only access)
E2E_VIEWER_EMAIL=
E2E_VIEWER_PASSWORD=

# =============================================================================
# OAUTH TEST USERS (Optional - for OAuth flow tests)
# =============================================================================
# These are only needed if you want to test actual OAuth login flows.
# Note: OAuth tests are typically skipped in CI due to 2FA requirements.

# GitHub OAuth test user
E2E_GITHUB_TEST_USER=
E2E_GITHUB_TEST_PASSWORD=

# Google OAuth test user
E2E_GOOGLE_TEST_USER=
E2E_GOOGLE_TEST_PASSWORD=

# =============================================================================
# TEST DATA CONFIGURATION (Optional)
# =============================================================================

# Organization ID for testing (leave empty to use default)
E2E_TEST_ORG_ID=

# Project ID for testing (leave empty to use default)
E2E_TEST_PROJECT_ID=

# =============================================================================
# TIMEOUTS AND RETRIES (Optional)
# =============================================================================

# Default timeout for assertions (ms)
E2E_TIMEOUT=30000

# Number of test retries on failure
E2E_RETRIES=1
